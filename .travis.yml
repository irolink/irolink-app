language: python
python:
  - 2.7
before_script:
  - pip install -r src/web/requirements.txt
script:
  - python -m unittest discover -s tests/ -p '*_test.py'
notifications:
  email:
    recipients: irolink@gmail.com
    on_success: always # [always|never|change]
    on_failure: always # [always|never|change]
